services:
  - type: web
    name: steam-manifest-backend
    env: node
    buildCommand: npm install && node server/db/migrate.js
    startCommand: node server/index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3001
